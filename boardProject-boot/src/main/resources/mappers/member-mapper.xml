<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.kh.project.member.model.mapper.MemberMapper">
	<!-- namespace에 연결할 Mapper 인터페이스 패키지명+인터페이스명 작성
		-> 매퍼 인터페이스 메서드 호출 시
			xml 파일에 작성된 태그중 메서드명과 같은 di를 가진 SQL이 수행됨
	 -->

   	<!-- <cache-ref namespace=""/> -->
   	<!-- 위에 cache 꼭 지우기 -->
   
   	<!-- parameterType 속성은 필수 속성 아님!
   		왜? TypeHandler 를 이용해서 자료형을 판별할 수 있음
    -->
   
   	<!-- 로그인 -->
   	<select id="login"  resultType="Member">
   		SELECT MEMBER_NO, MEMBER_EMAIL, MEMBER_NICKNAME, MEMBER_PW,
		MEMBER_TEL, MEMBER_ADDRESS, PROFILE_IMG, AUTHORITY,
		TO_CHAR(ENROLL_DATE, 'YYYY"년" MM"월" DD"일" HH24"시" MI"분" SS"초"') ENROLL_DATE
		FROM "MEMBER"
		WHERE MEMBER_EMAIL = #{memberEmail}
		AND MEMBER_DEL_FL = 'N'
   	</select>
   	<!-- DBconfig에서 별칭을 만들어서 Member만 써도 된다 -->
   
   	<!-- 이메일 중복 검사 -->
   	<select id="checkEmail" resultType="_int">
   		SELECT COUNT(*)
		FROM "MEMBER"
		WHERE MEMBER_DEL_FL = 'N'
		AND MEMBER_EMAIL = #{memberEmail}
   </select>
   
   
   <!-- MemberMapper.java 에서 메서드 명이랑 id값이랑 같아야한다 -->
   <select id="checkNickname" resultType="_int">
   		SELECT COUNT(*)
   		FROM "MEMBER"
   		WHERE MEMBER_DEL_FL = 'N'
   		AND MEMBER_NICKNAME = #{memberNickname}
   </select>
   
   
   <!-- 회원가입 -->
   <insert id="signup">
   		INSERT INTO "MEMBER"
   		VALUES(SEQ_MEMBER_NO.NEXTVAL,
	   		#{memberEmail},
	   		#{memberPw},
	   		#{memberNickname},
	   		#{memberTel},
	   		#{memberAddress},
	   		NULL,
	   		DEFAULT,
	   		DEFAULT,
	   		DEFAULT
   		)
   </insert>
   
   
   	<!-- 회원 목록 조회 -->
   	<!-- 구해진 모든행이 List<Member>에 모임 -->
	<select id="selectMemberList" resultType="Member">
		SELECT MEMBER_NO, MEMBER_EMAIL, MEMBER_NICKNAME, MEMBER_DEL_FL
		FROM "MEMBER"
		ORDER BY MEMBER_NO
	</select>
   
   
   	<!-- 비밀번호 초기화 -->
	<update id="resetPw">
		UPDATE "MEMBER" SET
		MEMBER_PW = #{encPw}
		WHERE MEMBER_NO = #{inputNo}
	</update>
   
   
   
   
</mapper>
